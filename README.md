# ⚡ JavaScript Engineering Core

A technical portfolio demonstrating the progression from functional algorithms to scalable, asynchronous frontend applications using modern **Vanilla JS** patterns.

## 📂 System Architecture

### Phase 1: Algorithmic Logic (ES6+)
**Focus:** Functional Programming, Data Structures & Utility Design.

| Module | Engineering Concept |
| :--- | :--- |
| `mfr-practice.js` | **Data Pipelines**: Complex transformations using `map`, `filter`, `reduce`. |
| `async-patterns.js` | **Event Loop**: Implementation of Promises, Callbacks, and execution context. |
| `data-inspector.js` | **Recursion**: Algorithms for deep object traversal and cloning. |
| `string-utils.js` | **Regex & Parsing**: Validation logic and string manipulation algorithms. |
| `math-utils.js` | **Computational Logic**: Helper functions for geometry and randomization. |

---

### Phase 2: DOM & Interactive Systems
**Focus:** Event Delegation, Browser Storage, and UI State.

| Application | Engineering Concept |
| :--- | :--- |
| **Expense Tracker** | **Dynamic Rendering**: DOM injection based on array state. |
| **Library System** | **OOP Design**: Utilizing ES6 Classes and Prototypal Inheritance. |
| **Notes App** | **Persistence**: CRUD operations interfacing with `localStorage`. |
| **Morse Code** | **Input Mapping**: Real-time event handling and dictionary lookups. |
| **Time Dashboard** | **Temporal Logic**: Synchronization using `Date` and `setInterval`. |

---

### Phase 3: Asynchronous State & APIs
**Focus:** Fetch API, Service Layers, and Performance Optimization.

| Application | Engineering Concept |
| :--- | :--- |
| **Infinite Gallery** | **Performance**: Lazy loading via `IntersectionObserver` API. |
| **GitHub Fetcher** | **API Integration**: RESTful data fetching with error boundaries. |
| **Kanban Fetcher** | **Complex State**: Asynchronous data hydration and UI synchronization. |

---

## 🛠 Technical Stack
*   **Core:** Vanilla JavaScript (ES6+), semantic HTML5.
*   **Styling:** CSS Variables, Flexbox, CSS Grid (No external frameworks).
*   **Key Patterns:**
    *   **Async/Await** for non-blocking operations.
    *   **Service-based Architecture** (Separating Data Fetching from UI).
    *   **Observer Pattern** (Intersection Observer).
    *   **Module Pattern** for scope isolation.

## 🚀 Execution
1.  Clone the repository.
2.  Serve the root or specific folder using **Live Server** (VS Code).
